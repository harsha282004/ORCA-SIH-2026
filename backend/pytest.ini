[pytest]
pythonpath = .
testpaths = tests
markers =
    integration: requires live PostgreSQL/PostGIS/Redis infrastructure (e.g. via docker compose)
    live: requires real internet access to an external API (e.g. Open-Meteo); no local infrastructure needed
